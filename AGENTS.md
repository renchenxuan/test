## Cursor Cloud specific instructions

This is a blank repository with only a `README.md` file. There is no application code, no dependencies, no services, and no build/test/lint tooling configured.

### Environment

- **Node.js**: Available via nvm (v22.x default)
- **Python**: 3.12.x
- **Git**: 2.43.x

### Notes

- No package manager lockfile exists; no dependency installation is required.
- No services to start, no tests to run, no lint checks configured.
- When application code is added, update this file with relevant dev setup, test, and run instructions.
