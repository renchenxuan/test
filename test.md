# test_清洗重排约束补偿.yml 链路检查与改进记录

## 1) 本次检查范围
- 文件：`/home/runner/work/test/test/test_清洗重排约束补偿.yml`
- 检查内容：
  - 全链路结构（节点、连线、变量引用）
  - 关键分支执行逻辑（单意图、多意图迭代、主辅库协同、ES 回退）
  - 迭代内节点配合关系与输出聚合正确性

## 2) 全链路概览（端到端）
- 起点：`1765884761572`（用户输入）
- 核心前置：
  - `llm`（意图识别）
  - `1765885847821`（意图提示词）
  - `1766046797761`（意图参数解析，产出 `sub_intents` 等）
- 主路由：
  - `1766543031078`（意图分流）
    - 闲聊分支 -> 闲聊 LLM -> 直接回复
    - 单意图知识分支 -> 主/辅知识库检索与融合 -> 回复
    - 多意图分支 -> `1766561987023` 迭代逐子意图处理 -> 汇总 LLM -> 回复
    - 其他异常/澄清分支 -> 对应回复

## 3) 关键节点协作逻辑

### 3.1 单意图链路（简述）
- 通过条件分支判断检索模式（向量、ES、混合等）。
- 主库与辅库分别按知识域检索（CBB/专利/论文/标准/流程规范），再聚合融合。
- 对异常或无结果有兜底节点（异常回复）。

### 3.2 多意图迭代链路（重点）
- 迭代节点：`1766561987023`
  - 输入：`1766046797761.sub_intents`（数组对象）
  - 每轮处理单个子意图：
    - 子意图参数抽取（`1766562647925`）
    - 闲聊 / 检索 / ES 分支判定（`1766573655896`）
    - 检索分支中主辅库召回、证据合并（`1766579029694`）
    - 各分支输出统一汇入“聚合输出”节点（`1766632716395`）
  - 迭代输出再由 `1766583988064` 统一拼装，送到 `1766643827180` 总结 LLM 生成最终回复。

## 4) 本次发现的问题与最终策略

### 问题：召回片段提炼链路不符合当前目标方案
- 当前目标为：删除“召回片段提炼LLM”及其相关节点，采用方案2/3/4混合策略。
- 因此不再保留提炼子链路（`1766661968545`、`1766663294206`）参与迭代输出。

### 最终修复动作（按当前目标）
- 删除上一版中的提炼链路节点（当前文件已不存在）：
  - `1766661968545`（召回片段提炼LLM）
  - `1766663294206`（取出提炼内容）
- 删除上一版中的提炼链路连线（当前文件已不存在）：
  - `1766579029694 -> 1766661968545`
  - `1766661968545 -> 1766663294206`
  - `1766663294206 -> 1766632716395`
- 恢复聚合输入为混合方案：
  - `1766632716395` 第二路输入改回 `1766579029694.evidence_pack`
  - 由 `1766579029694 -> 1766632716395` 直连参与聚合

## 5) 修复后验证结论
- YAML 结构可正常解析。
- 节点与边引用完整（无缺失端点）。
- 模板变量引用有效（未发现指向不存在节点的模板引用）。
- 迭代内不再包含召回片段提炼LLM链路，聚合节点输入与“方案2/3/4混合”目标一致。

## 6) 后续改进建议（供下一步迭代）
- 为关键聚合节点增加命名规范说明（例如：输入1=闲聊、输入2=主辅证据包、输入3=ES、输入4=异常），便于后续维护。
- 在升级说明文档中补充“多意图迭代链路数据流图”，降低后续改动风险。
